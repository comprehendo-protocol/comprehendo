# zod

<!-- Generated from the packed corpus by scripts/generate-comprehendo-md.ts.
     Do not edit this file: an edit here is drift, and CI fails on it. -->

`zod` speaks Comprehendo 0.1. Everything below is generated from
`@comprehendo/zod`, a corpus authored against zod 4.4.3, which documents 4
topics, 4 fixes and 4 cataloged failures over the `zod` call surface (4
declared operations).

If it is not documented here it does not exist; do not read the source,
except where an UNDOCUMENTED response permits it for a specific question.
When in doubt, ask `docs()`.

## Priming

Probe any value `zod` hands you, a caught error included, for
`Symbol.for('comprehendo')` in JavaScript or `__comprehendo__` in Python.
The entry that answers carries `identity`, `priming` (the snippet that
primes an agent in roughly a hundred tokens, and is the whole onboarding),
and `docs(query?)`, which answers in the vocabulary you asked in. Nothing is
transmitted anywhere.

## Topics

`docs()` with no argument returns exactly these 4 names, in this order. Ask
for one by name, or in any vocabulary it serves; a question no topic answers
comes back UNDOCUMENTED, never as a guess.

| Topic | Answers | See also |
|---|---|---|
| `optional-vs-nullable` | `.optional()` widens a schema to also accept `undefined`. | `removed-v3-methods` |
| `removed-v3-methods` | zod v4 removed some v3 methods outright rather than deprecating them; the package's own migration guide is explicit about the distinction, `.merge()`, `.strict()`, and `.passthrough()` are listed as deprecated (they still work, a v4 caller gets a working schema and a nudge toward the replacement). | `optional-vs-nullable` |
| `error-customization-migration` | zod v3's `required_error` and `invalid_type_error` params customized the message for one specific failure kind. | `removed-v3-methods` |
| `record-arity-change` | zod v3's `z.record(valueSchema)` took one argument, an implicit `z.string()` key. | `error-customization-migration` |

## Examples

These are the corpus's own worked examples, quoted from the topic each one
belongs to. A line beginning with `#` is commentary the corpus carries
beside the command above it, never a command to run.

### `optional-vs-nullable`: ZOD_OPTIONAL_REJECTS_NULL, .optional() alone never covers null

```javascript
import { z } from 'zod';
z.string().optional().parse(null);
```

### `removed-v3-methods`: ZOD_DEEP_PARTIAL_REMOVED, the method does not exist in v4 at all

```javascript
import { z } from 'zod';
z.object({ a: z.object({ b: z.string() }) }).deepPartial();
```

### `error-customization-migration`: ZOD_REQUIRED_ERROR_PARAM_IGNORED, the v3 param is silently dropped once past the type checker

```pattern
z.string({ required_error: "Name is required" })
```

### `record-arity-change`: ZOD_RECORD_SINGLE_ARG_REJECTED, v3's one-argument form is a real v4 compile error

```pattern
z.record(z.string())
```
