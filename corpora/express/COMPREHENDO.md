# express

<!-- Generated from the packed corpus by scripts/generate-comprehendo-md.ts.
     Do not edit this file: an edit here is drift, and CI fails on it. -->

`express` speaks Comprehendo 0.1. Everything below is generated from
`@comprehendo/express`, a corpus authored against express 5.2.1, which
documents 4 topics, 5 fixes and 5 cataloged failures over the `express` call
surface (9 declared operations).

If it is not documented here it does not exist; do not read the source,
except where an UNDOCUMENTED response permits it for a specific question.
When in doubt, ask `docs()`.

## Priming

Probe any value `express` hands you, a caught error included, for
`Symbol.for('comprehendo')` in JavaScript or `__comprehendo__` in Python.
The entry that answers carries `identity`, `priming` (the snippet that
primes an agent in roughly a hundred tokens, and is the whole onboarding),
and `docs(query?)`, which answers in the vocabulary you asked in. Nothing is
transmitted anywhere.

## Topics

`docs()` with no argument returns exactly these 4 names, in this order. Ask
for one by name, or in any vocabulary it serves; a question no topic answers
comes back UNDOCUMENTED, never as a guess.

| Topic | Answers | See also |
|---|---|---|
| `removed-methods` | Express 5's own migration guide lists a real "Removed" section, distinct from methods that are merely deprecated (still work, print a warning). | `route-pattern-syntax` |
| `route-pattern-syntax` | Express 5 bumped its underlying path-matching dependency from `path-to-regexp` v6 to v8, a real, documented breaking change to route STRING syntax made by that dependency, not by Express itself. | `removed-methods` |
| `res-send-status-codes` | In Express 4, calling `res.send()` with a bare NUMBER (`res.send(404)`) was a deprecated alias for `res.sendStatus(404)`: it really set the response status code, and printed a real deprecation warning naming the intended replacement. | `removed-methods` |
| `async-error-handling` | In Express 4, a promise rejected inside an async route handler was NOT automatically caught. |  |

## Examples

These are the corpus's own worked examples, quoted from the topic each one
belongs to. A line beginning with `#` is commentary the corpus carries
beside the command above it, never a command to run.

### `removed-methods`: EXPRESS_APP_DEL_REMOVED, gone at registration time

```javascript
import express5 from 'express5';
const app = express5();
app.del('/x', () => undefined);
```

### `removed-methods`: EXPRESS_REQ_PARAM_REMOVED, gone at request time

```javascript
import express5 from 'express5';
const req = express5.request;
req.param('id');
```

### `route-pattern-syntax`: EXPRESS_BARE_WILDCARD_REMOVED, a bare wildcard needs a name now

```javascript
import express5 from 'express5';
const app = express5();
app.get('*', (req, res) => res.end());
```

### `route-pattern-syntax`: EXPRESS_OPTIONAL_PARAM_SYNTAX_REMOVED, the `?` suffix is no longer valid syntax

```javascript
import express5 from 'express5';
const app = express5();
app.get('/foo/:bar?', (req, res) => res.end());
```

### `res-send-status-codes`: EXPRESS_RES_SEND_NUMBER_NO_LONGER_SETS_STATUS, silently the wrong response now

```pattern
res.send(404)
```
