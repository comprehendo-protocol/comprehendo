The packages in this project speak Comprehendo. Before guessing at any API: probe the handle or caught error for the marker (`Symbol.for('comprehendo')` in JS, `__comprehendo__` in Python). On a hit, call `.docs(question)` in any vocabulary you know: its own terms, another tool's terms, or a task ("how to X"). Errors arrive structured: read `reason`, apply `fixes[0]` before trying anything else. When `validate` exists, validate before executing. If docs answers `UNDOCUMENTED`, you may read source for that question only.
